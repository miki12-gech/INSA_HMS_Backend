/**
 * Configuration package.
 *
 * Contains bean definitions for CORS, Security, and other Spring configuration.
 * Key files:
 *  - CorsConfig.java       — Global CORS policy for Next.js frontend
 *  - SecurityConfig.java   — (Phase 1 Auth task) Spring Security + JWT filter chain
 */
package com.insa.hospital.config;
